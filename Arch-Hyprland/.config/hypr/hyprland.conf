# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto

#
###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = alacritty
$fileManager = nemo
$wMenu = wofi --show drun
$rMenu = rofi -show drun

#################
##a AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox



#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
#source = /home/sloth/.cache/wal/colors.json
general {
    gaps_in = 4
    gaps_out = 12

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    #col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    #col.inactive_border = rgba(595959aa)

    
    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
  # Optional: allow tearing for performance try make it run always high refresh rate
  allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 5
    

    # Change transparency of focused and unfocused windows
    active_opacity = 0.8
    inactive_opacity = 0.6



    shadow {
        enabled = true
        range = 8
        render_power = 10
        color = rgba(0, 0, 0, 1)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 0 # it makes it look  pixelated
        passes = 4 # applies blur (Default: 3)

        vibrancy = 0.2
        #ignore_opacity  = true

    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1


    animation = windows, 1, 5, myBezier
    animation = windowsIn, 1, 3, myBezier, slide   
    animation = windowsOut, 1, 5, myBezier, slide  
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 7, default
    animation = layers, 1, 3, myBezier
    animation = layersIn, 1, 3, myBezier, slidevert 
    animation = layersOut, 1, 2, myBezier, slidefade 
    animation = workspaces, 1, 3, myBezier, slide



    #animation = global, 1, 10, default
    #animation = border, 1, 5.39, easeOutQuint
    #animation = windows, 1, 4.79, easeOutQuint
    #animation = windowsIn, 1, 2.1, easeOutQuint, popin 87%
    #animation = windowsOut, 1, 2.1, linear, popin 87%
    #animation = fade, 1, 3.03, quick
    #animation = layers, 1, 3.81, easeOutQuint
    #animation = layersIn, 1, 4, easeOutQuint, slidefade
    #animation = layersOut, 1, 1.5, linear, fade
    #animation = fadeLayersIn, 1, 1.79, almostLinear
    #animation = fadeLayersOut, 1, 1.39, almostLinear
    #animation = workspaces, 1, 1.94, almostLinear, fade
    #animation = workspacesIn, 1, 1.21, almostLinear, fade
    #animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers set -1 to on it
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
    force_no_accel = 0 
    touchpad {
        natural_scroll = true
    }
}

#Identify your input devices
#-- hyprctl devices

device {
  name = logitech-usb-optical-mouse
  sensitivity = -0.90            # adjust between -1.0 (slower) to +1.0 (faster)
  accel_profile = "flat"        # "flat" for no acceleration, "adaptive" for default accel
}


device {
  name = dell08b8:00-0488:121f-touchpad
  sensitivity = 0.0             # default speed for touchpad
  accel_profile = "flat"    # default behavior with acceleration
}


# https://wiki.hyprland.org/Configuring/Variables/#gestures
#gestures {
#    workspace_swipe = false
#}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod SHIFT, E, exit,
bind = $mainMod SHIFT, SPACE, togglefloating,
bind = $mainMod, D, exec, $rMenu
bind = $mainMod SHIFT, D, exec, $wMenu
bind = $mainMod, P, pseudo, # dwindle make the windows to open like floaing window 
bind = $mainMod SHIFT, R, exec, hyprctl reload  &&  ~/.config/scripts-arch/full-reload.sh
bind = $mainMod, E, togglesplit, # Toggle between stack or tb window to default




# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, j, movefocus, d
bind = $mainMod, k, movefocus, u
bind = $mainMod, l, movefocus, r

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod CTRL, F, togglespecialworkspace, magic
bind = $mainMod SHIFT, F, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow





# Resize with SUPER + R
bind = $mainMod, R, submap, resize
submap = resize

binde = , h, resizeactive, -10 0      
binde = , l, resizeactive, 10 0       
binde = , k, resizeactive, 0 -10      
binde = , j, resizeactive, 0 10 
      
bind = , escape, submap, reset
bind = , return, submap, reset
submap = reset

#or
#binde = , right, resizeactive, 50 0   
#binde = , left, resizeactive, -50 0    
#binde = , up, resizeactive, 0 -50      
#binde = , down, resizeactive, 0 50
#binde = SHIFT, right, resizeactive, 10 0  
#binde = SHIFT, left, resizeactive, -10 0 
#binde = SHIFT, up, resizeactive, 0 -10    
#binde = SHIFT, down, resizeactive, 0 10






# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
# to unmute - pactl set-sink-mute 0 0
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, /usr/bin/brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, /usr/bin/brightnessctl -e4 -n2 set 5%-


# Requires playerctl
#bindl = , XF86AudioNext, exec, playerctl next
#bindl = , XF86AudioPause, exec, playerctl play-pause
#bindl = , XF86AudioPlay, exec, playerctl play-pause
#bindl = , XF86AudioPrev, exec, playerctl previous



######  ScreenShot  ######

# Fullscreen screenshot (save to ~/Pictures/)
bind = , Print, exec, grim ~/Pictures/Screenshots/screenshot_$(date +'%Y-%m-%d-%H%M%S').png

# Select area (copy to clipboard)
bind = SHIFT, Print, exec, grim -g "$(slurp)" - | wl-copy

# Active window screenshot
bind = ALT, Print, exec, grim -g "$(hyprctl activewindow | grep 'at:' | awk '{print $2}')" ~/Pictures/Screenshots/window_$(date +'%Y-%m-%d-%H%M%S').png


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0


##############################
#######   Task Bar rel.#######
##############################

#waybar - to run different config and css use:
#  exec-once = waybar -c ~/.config/waybar/config -s ~/.config/waybar/style.css
exec-once = waybar


#############################
#######  wallpaper  #########
#############################


## rofi theme wall ##
exec-once = swww-daemon --format xrgb
exec-once = swww init
exec-once = swww img ~/.config/hypr/themes/theme1/wallpapers/default.jpg --transition-fps 60 --transition-step 255 --transition-type outer
exec-once = ~/.config/hypr/scripts/auto_change_wallpaper.sh
exec-once = ~/.config/hypr/scripts/update-rofi-background.sh


#5 minutes  =	sleep 300
#10 minutes =	sleep 600
#30 minutes =	sleep 1800
#1 hour	    =   sleep 3600

###  pywal  ###


#exec-once = swww-daemon --format xrgb
#exec-once = bash -c 'while true; do IMG="$(find /home/sloth/wallpaper/shuffle/ -type f | shuf -n 1)"; swww img "$IMG"; wal -i "$IMG" -n -q; sleep 600; done' &



# To reload waybar & pywalfox automatically each time pywal gives it a new color:
exec-once = /home/sloth/.config/scripts-arch/watch-pywal-refresh.sh




############################
#####  Lock Screen  ########
############################

exec-once = swayidle -w \
  timeout 1800 '~/.config/scripts-arch/lock.sh' \
  timeout 1815 'hyprctl dispatch dpms off' \
  after-resume 'sleep 1 && hyprctl dispatch dpms on' \
  before-sleep '~/.config/scripts-arch/lock.sh' \
  lock '~/.config/scripts-arch/lock.sh'

bind = $mainMod, semicolon, exec, ~/.config/scripts-arch/lock.sh



############################
######  WiFi config  #######
############################

exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP HYPRLAND
#exec-once = nm-applet --indicator


##############################
#######  GTK Themes  #########
##############################

exec-once = nwg-look -a  # Applies saved settings at startup

#nwg-look -x  # Export current theme config
#nwg-look -r  # Reset to defaults

###############################
######  App key bind  #########
###############################


bind = $mainMod, B, exec, google-chrome-unstable
bind = $mainMod, O, exec, opera
bind = $mainMod, C, exec, code
bind = $mainMod, N, exec, xed
bind = $mainMod SHIFT, M, exec, nautilus
bind = $mainMod, F, exec, firefox 
bind = $mainMod, M, exec, nemo
bind = $mainMod, T, exec,  /home/sloth/.config/hypr/scripts/select_theme.sh
bind = $mainMod, W, exec, /home/sloth/.config/hypr/scripts/select_wallpaper_rofi.sh
bind = $mainMod SHIFT, W, exec, /home/sloth/.config/hypr/scripts/change_wallpaper_next.sh
